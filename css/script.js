// script.js
document.addEventListener('DOMContentLoaded', (event) => {
    // DOM is fully loaded, but maybe waiting on images & stylesheets
    // Initial script setup or event listeners can go here
});
